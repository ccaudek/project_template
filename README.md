# Project template

R script for generating the folder structure of an R project.

- Source `project_template.R` after changing the working directory. 
- Change the working directory so that it corresponds to a newly created folder having the name of the new project.



